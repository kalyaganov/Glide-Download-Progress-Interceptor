package ru.futurobot.glidedownloadinterceptor

import android.app.Application

/**
 * Application class for the demo app.
 *
 * With Glide 4.x, [MyAppGlideModule] is auto-discovered via the annotation
 * processor, so no manual OkHttp registration is needed here.
 */
class MainApplication : Application()
